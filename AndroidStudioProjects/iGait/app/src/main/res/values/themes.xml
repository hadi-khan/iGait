<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="BookBlueActionBarTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/BookBlueActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/BookBlueActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="BookBlueActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/BookBlue  </item>

        <!-- Support library compatibility -->
        <item name="background">@color/BookBlue</item>
    </style>
</resources>